SET QUOTED_IDENTIFIER OFF
GO
SET ANSI_NULLS ON
GO





CREATE          VIEW [dbo].[S_DFUPAG] AS 

SELECT TOP 100 PERCENT 
       KOD,KMON=MAX(KMON),VLEFTAPAG=SUM(VLEFTA),VLEFTAMVPAG=SUM(VLEFTAMV) 
  FROM DFU
 WHERE TREGDK='D'
GROUP BY KOD
ORDER BY KOD














GO
