SET QUOTED_IDENTIFIER ON
GO
SET ANSI_NULLS ON
GO





CREATE  procedure [dbo].[MG_DELMG]
(
@PTABLENAME VARCHAR(10),
@PNRD INT
)
AS
DECLARE @STR AS VARCHAR(500)

SET @STR='DELETE FROM ' +  @PTABLENAME + ' WHERE NRRENDOR=' + CONVERT(VARCHAR,@PNRD)

EXECUTE(@STR)



GO
